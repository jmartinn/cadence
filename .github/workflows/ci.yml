name: CI

on:
  pull_request:
    branches: [master]
  push:
    branches: [master]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: verify
    runs-on: macos-15
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Select Xcode
        uses: maxim-lobanov/setup-xcode@ed7a3b1fda3918c0306d1b724322adc0b8cc0a90 # v1
        with:
          xcode-version: latest-stable

      - name: Diagnostics (temporary — removed once pins are finalized)
        run: |
          set -euo pipefail
          echo "## Xcode"; xcodebuild -version
          echo "## iOS SDKs"; xcodebuild -showsdks | grep -i ios || true
          echo "## Simulator runtimes"; xcrun simctl list runtimes | grep -i ios || true
          echo "## iPhone simulators"; xcrun simctl list devices available | grep -i iphone || true
