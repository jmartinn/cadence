import XCTest

/// UI test target scaffold. The Xcode template's boilerplate tests (which
/// asserted nothing and flaked CI) were removed. CI skips this target until
/// real UI smoke coverage exists — see `.github/workflows/ci.yml`.
final class CadenceUITests: XCTestCase {}
